#include<iostream>
#include<cstring>
using namespace std;
int n,k;
int main(int argc,char* argv[]){
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    cin>>n>>k;
    int*text=new int[n],*def=new int[n];
    for(int i=0;i<n;i++){
        text[i]=i+1;
        def[i]=1;
    }
    while(k--){
        int x,y,z,sum;
        cin>>x>>y>>z;
        
    }
    return 0;
}